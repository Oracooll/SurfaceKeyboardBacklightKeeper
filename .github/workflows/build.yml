name: Build

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build with the in-box .NET Framework C# compiler
        shell: pwsh
        run: powershell -NoProfile -ExecutionPolicy Bypass -File .\build.ps1
      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: SurfaceKeyboardBacklightKeeper
          path: build/SurfaceKeyboardBacklightKeeper.exe
